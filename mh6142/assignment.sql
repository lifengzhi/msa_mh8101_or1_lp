
later add the foreign key reference to club

create table PLAYERS(
    player_id int NOT NULL,
    dob date NOT NULL,
    position VARCHAR(64) NOT NULL,
    salary float NOT NULL,
    clubname VARCHAR(64)); 
    

